# ---------------------------------------------------------------------------
# Local development profile for the Ride Service.
# Activate with: SPRING_PROFILES_ACTIVE=local
# ---------------------------------------------------------------------------

spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.ridebooking.ride: DEBUG
    org.hibernate.SQL: DEBUG