# ---------------------------------------------------------------------------
# Local development profile for the Payment Service.
# Activate with: SPRING_PROFILES_ACTIVE=local
# ---------------------------------------------------------------------------

spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.ridebooking.payment: DEBUG
    org.hibernate.SQL: DEBUG